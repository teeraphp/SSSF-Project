type User {
  id: ID!
  username: String!
  email: String!
}

type Query {
  users: [User!]!
  userById(id: ID!): User
}

type Mutation {
  createUser(username: String!, email: String!): User!
  updateUser(id: ID!, username: String, email: String): User!
}
